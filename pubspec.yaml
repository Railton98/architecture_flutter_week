name: architecture_flutter_week
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flutter_modular: ^1.2.5+1
  core_module:
    path: modules/core_module
  login_module:
    path: modules/login_module
  home_module:
    path: modules/home_module

dev_dependencies:
  mockito: ^4.1.1
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
